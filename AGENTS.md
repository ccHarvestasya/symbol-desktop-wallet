# リポジトリの役割

このリポジトリは、Symbol Desktop Wallet の保守に関するオーケストレーションおよびプロジェクト管理用のワークスペースです。

## 実装リポジトリ

実際のアプリケーションソースコードは、次のディレクトリにあります。

    ./desktop-wallet

`desktop-wallet/` は独立したGitリポジトリです。

本番アプリケーションの実装変更は、必ず `desktop-wallet/` 内で行ってください。

オーケストレーションリポジトリには、アプリケーションのソースコードを実装しないでください。

## オーケストレーションリポジトリ

親リポジトリ `symbol-desktop-wallet` は、次の用途に使用します。

- プロジェクト計画
- 調査メモ
- セキュリティ分析
- 要件整理
- 実装計画
- レビュー指摘
- 移行計画
- 進捗管理

管理用の成果物は `docs/` 配下に保存してください。

## Gitリポジトリの境界

2つのGitリポジトリは、常に別々に扱ってください。

アプリケーションを変更する場合:

    cd desktop-wallet

Gitコマンドの実行、ブランチ作成、コミット、プッシュは、`desktop-wallet/` リポジトリ内で行ってください。

アプリケーションのソースコード変更を親リポジトリにコミットしないでください。

管理資料やドキュメントを変更する場合は、親リポジトリを使用してください。

## 言語とコミットメッセージ

親リポジトリ（`symbol-desktop-wallet`）の成果物は、すべて日本語で作成してください。

- `AGENTS.md`、`docs/` 配下の計画・調査・レビュー資料は日本語で書く。
- 親リポジトリのコミットコメントは日本語で書く。
- コミットコメントには、何を変更したか分かるように、変更内容を箇条書きで詳細に記載する。
- 親リポジトリで英語の固有名詞、コード識別子、外部仕様上の名称を記載する場合は、必要最小限にとどめる。

`desktop-wallet/` 配下の成果物は、すべて英語で作成してください。

- アプリケーションのソースコード内のコメント、ドキュメント、テスト名などは英語で書く。
- `desktop-wallet` リポジトリのコミットコメントは英語で書く。
- コミットコメントには、何を変更したか分かるように、変更内容を箇条書きで詳細に記載する。

## 作業手順

実装作業では、次の手順に従ってください。

1. 親リポジトリ内の関連資料を読む。
2. `desktop-wallet/` の現在の状態を確認する。
3. `desktop-wallet/` 内で実装作業を行う。
4. 適切なテスト、Lint、型チェック、ビルドを `desktop-wallet/` 内で実行する。
5. 実装変更を `desktop-wallet` リポジトリでコミットする。
6. 必要に応じて、親リポジトリの進捗・レビュー資料を更新する。
7. 2つのリポジトリのHEADを分けて報告する。

常に次の2つを区別してください。

- オーケストレーションリポジトリのHEAD
- `desktop-wallet` リポジトリのHEAD
